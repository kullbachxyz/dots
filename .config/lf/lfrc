# Basic settings
set icons
set cleaner '~/.config/lf/cleaner'
set previewer '~/.config/lf/scope'

# Bindings
map H cd ~
map D delete
map gg top
map G bottom

map gw cd ~/.local/share/walls

map bg $setbg $f
